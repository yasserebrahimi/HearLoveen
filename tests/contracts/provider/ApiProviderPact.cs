using System.Threading.Tasks;
using Xunit;
namespace HearLoveen.Contracts.Provider;
public class ApiProviderPact
{
    [Fact(Skip="Enable after adding PactNet")] public async Task Verifies_contracts(){ await Task.CompletedTask; }
}
